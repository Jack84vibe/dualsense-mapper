# ============================================================
#  DualSense Mapper - .gitignore
#  規則：只要是「別人 clone 下來能自己重新產生的東西」，就不上傳。
# ============================================================

# --- 虛擬環境（run.bat 會自動重建，幾百 MB，絕對不能傳）---
/.venv/
.venv/
venv/
env/

# --- Python 快取 ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/

# --- 打包產物（release.bat 會自己生，含 21MB 的 exe）---
/output/
build/
dist/
*.spec

# --- 你的個人配置（程式第一次執行會自己生成一份）---
#  這份是「你的」按鍵綁定、燈光顏色、扳機設定，不該變成別人的預設值。
#  出廠預設值走 ps5mapper/default_config.json，那個要上傳。
/ps5mapper.config.json

# --- 清理用的暫存區 ---
/_to_delete/

# ============================================================
#  沒有 probe 相關的排除規則，因為 v1.0 時全部刪掉了。
#
#  探測腳本和實測報告是開發過程的產物，不是產品的一部分。
#  每個寫死的常數為什麼是這個值，都寫在它自己旁邊的註解裡
#  （gyro.py、dualsense.py、lights.py、engine.py），
#  而且那些註解用的是當前的單位和當前的設計 —— 比舊報告準。
#
#  將來要測新東西，寫新的腳本、放在根目錄、測完刪掉，
#  結論寫進程式碼註解。不要再累積一批歷史報告。
# ============================================================

# ============================================================
#  編輯器 / 系統雜物
# ============================================================
.vscode/
.idea/
*.swp
*.swo
*~

Thumbs.db
desktop.ini
ehthumbs.db
.DS_Store

# --- 臨時檔 ---
*.log
*.tmp
*.bak
